# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# AI Agent Configuration (Optional - will auto-resolve from database if not set)
# The MCP server automatically finds active AI agents from the database
# Only set AGENT_ID if you want to override the automatic agent selection
AGENT_ID=

# MCP Server Configuration
MCP_SERVER_NAME=crm-tasks-server
MCP_SERVER_VERSION=1.0.0
MCP_LOG_LEVEL=info
