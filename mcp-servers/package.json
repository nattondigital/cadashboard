{
  "name": "crm-mcp-servers",
  "version": "1.0.0",
  "description": "MCP servers for CRM system with custom OTP authentication",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev:tasks": "node --loader ts-node/esm tasks-server/index.ts",
    "dev:contacts": "node --loader ts-node/esm contacts-server/index.ts",
    "test:client": "node --loader ts-node/esm test-client.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["mcp", "crm", "tasks", "ai"],
  "author": "AI Academy Admin",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@supabase/supabase-js": "^2.58.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
